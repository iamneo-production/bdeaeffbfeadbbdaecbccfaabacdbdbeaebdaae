package service;

public interface InterfaceClass {
    double withdrawAmount(double availableBalance, double withdrawAmount);
    double depositAmount(double availableBalance, double depositAmount);
    double accountBalance(double availableBalance);
}
